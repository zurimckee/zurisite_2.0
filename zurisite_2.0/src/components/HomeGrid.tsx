import Header from './Header';
import { useNavigate } from 'react-router-dom';


type AppIconProps ={
  label: string;
  route: string;
  iconLink: string;
}

const AppIcon = ({label, route, iconLink}: AppIconProps) => {
  
}